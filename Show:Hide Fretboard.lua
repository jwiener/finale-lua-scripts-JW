function plugindef()   -- This function and the 'finaleplugin' namespace   -- are both reserved for the plug-in definition.   finaleplugin.Author = "Jesse Wiener"   finaleplugin.Version = "0.1"   return "Show/Hide Fretboard", "Undo Show/Hide Fretboard", "Shows/Hides Fretboard for Selected Bar"end--this returns the measure and staff numberfor m, s in eachcell(finenv.Region()) doprint (m, s)	local chords = finale.FCChords()        chords:LoadAllForRegion(finenv.Region())    for c in each (chords) do     	if c.FretboardVisible == true then    		c.FretboardVisible = false      c:Save()    	else    	c.FretboardVisible = true      c:Save()      end    --I moved the save command from here into the above for-loop     --and I think it works correctly now    --update: I forgot to put the save command in the first in statement! Now it works.  	endend